import 'package:flutter/material.dart';

class Const {
  static const String icon = "icons";
  static const String locationBox = "locations";
  static const String attendanceBox = "attendances";
  static double mediaHeight(BuildContext context) => MediaQuery.of(context).size.height;
  static double mediaWidth(BuildContext context) => MediaQuery.of(context).size.width;
}